/* import TransactionModel from './model.js';
import TransactionView from './view.js';
import TransactionController from './controller.js';

const model = new TransactionModel();
const view = new TransactionView();
new TransactionController(model, view);
 */

/* const app = new ExpenseController(new TodoService(), new TodoView()); */